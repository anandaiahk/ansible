- name: demo on filters
  hosts: local
  connection: local
  vars:
    topics: shell, linux, terraform, command
  tasks:
  - name: greeting
    ansible.builtin.debug:
      msg: hi anand ,{{ GREETING | default("good morning") }}

  - name: converting string to list
    ansible.builtin.debug:
      msg: "{{ topics | split(',') }}"    

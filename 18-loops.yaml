- name: demo on loops
  hosts: local
  connection: local
  become: yes
  tasks:
  - name: install package
    ansible.builtin.package:
      name: "{{ name.item }}"
      state: "{{ state.item }}"
    loop:
    - { name: "mysql", state: "absent" }
    - { name: "nginx", state: "present" }  